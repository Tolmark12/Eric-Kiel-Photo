<%= seventy_thirty_container do |c| %>
  <%= c.left do %>  
    <%= bento_form([:bento, @stock_default_category]) do |f| %>
      <% if @stock_default_category.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@stock_default_category.errors.count, "error") %> prohibited this stock_default_category from being saved:</h2>
    
          <ul>
          <% @stock_default_category.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
          <% end %>
          </ul>
        </div>
      <% end %>
      <%= f.group :default, {} do |g|%> 
        <div class="left-container">
          <%= g.label :text %>
		</div>
          <%= g.text_field :text %><br />
                		<div class="left-container">
          <%= g.label :src %>
		</div>
          <%= g.image_uploader :src %><br />
        <div class="left-container">
          <%= g.label :search_term %>
		</div>
          <%= g.text_field :search_term %><br />
      <% end %>
      <%= f.submit %>
    <% end %>
  <% end %>
  <%= c.right do %>
  <% end %>
<% end %>
